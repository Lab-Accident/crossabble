export enum GameStatus {
    NotStarted = 'not_started',
    InProgress = 'in_progress',
    Completed = 'completed'
}

export enum PlayerRole {
    Host = 'host',
    Player = 'player',
    Spectator = 'spectator'
}
